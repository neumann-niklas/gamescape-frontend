import { GroupPhasePipe } from './group-phase.pipe';

describe('GroupPhasePipe', () => {
  it('create an instance', () => {
    const pipe = new GroupPhasePipe();
    expect(pipe).toBeTruthy();
  });
});
